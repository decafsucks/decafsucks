steps:
  - label: Run tests
    commands:
      - bin/hanami db create -e test
      - bin/hanami db migrate -e test
      - bundle exec rake
    plugins:
      - docker-compose#v3.10.0:
          config: .buildkite/docker-compose.yml
          run: app
